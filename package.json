{
  "name": "sanaciones-web",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "parcel src/*.html",
    "build": "rimraf public/* && parcel build src/*.html --dist-dir public --no-source-maps --no-cache --public-url ./",
    "deploy": "rimraf public/* && parcel build src/*.html --dist-dir public --no-source-maps --no-cache --public-url ./ && gh-pages -d public"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.6.2",
    "gh-pages": "^4.0.0",
    "parcel": "^2.6.2",
    "rimraf": "^3.0.2"
  }
}